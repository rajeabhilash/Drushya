import 'package:flutter/material.dart';

class PlayWithColors extends StatelessWidget {
  const PlayWithColors({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
